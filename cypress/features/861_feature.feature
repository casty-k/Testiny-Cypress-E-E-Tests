Feature: 861
  861 feature scenarios

@861 @medium
Scenario: Only ready vehicles can be picked
    Then Only vehicles meeting that bar for their type appear as options — a vehicle that isn't ready is never shown as available.
