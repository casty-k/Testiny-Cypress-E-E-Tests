Feature: 1220
  1220 feature scenarios

@1220 @medium
Scenario: Close blocked with attempt but no two-way touch-point on normal path
    Then AMBIGUOUS IN SOURCE DOC — CO-SUP-03 §8 says "on the normal path" requires >=1 two-way touch-point; confirm with dev whether this is enforced as a second gate distinct from the contact-attempt gate before asserting pass/fail
