Feature: 847
  847 feature scenarios

@847 @medium
Scenario: Queue filterable by lane
    Then The queue is filterable by lane (prospect / win-back / active-customer) and shows each case's reason, disposition, and — for transient cases — the date or event it's waiting on.
