Feature: 961
  961 feature scenarios

@961 @medium
Scenario: Onboarding-exit (GW/Partner/Training cause) — priority re-warm, no penalty
    Then The case is routed to a priority re-warm with no suppression, no penalty, and no cooling-off period, and is flagged for direct agent contact rather than a standard drip. Re-entry, when it happens, is at CO-ACQ-01 for a fresh run, same as every other class.
